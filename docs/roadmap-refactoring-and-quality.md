# Дорожная карта: рефакторинг и повышение качества

## Цель

Сделать ChoKuda проще в поддержке, безопаснее для дальнейшей разработки и устойчивее к ошибкам без изменения пользовательского поведения.

Главный фокус: постепенно разгрузить большой Razor-компонент, закрепить ключевые сценарии тестами, привести ошибки в UI к понятному виду и подготовить файловое хранилище к будущим изменениям формата.

## Принципы работы

- Не переписывать все сразу. Каждый шаг должен быть маленьким, проверяемым и откатываемым.
- Не менять поведение приложения во время рефакторинга без отдельного решения.
- Сначала фиксировать текущее поведение тестами и ручными проверками, потом переносить код.
- `Core` хранит правила предметной области и работу с данными.
- App-level VM/сервисы хранят состояние экрана, команды UI и orchestration между `Core` и Razor.
- Razor-компоненты должны отвечать за разметку, биндинги и простые UI-события.
- После каждого этапа приложение должно собираться, тесты должны проходить, ручной smoke-check должен быть выполнен.

## Текущее состояние

- Основной экран стартует из `src/ChoKuda.App/Components/AppShell.razor`, но крупные UI-блоки уже вынесены в дочерние Razor-компоненты.
- `AppShell.razor` все еще содержит orchestration и часть состояния экрана:
  - выбор и инициализация библиотеки;
  - загрузка точек и коллекций;
  - поиск;
  - фильтрация;
  - работа с картой;
  - правая панель точки;
  - создание, редактирование и удаление точек;
  - создание, редактирование и удаление коллекций;
  - импорт, удаление и открытие вложений;
  - отображение ошибок.
- В `Core` уже есть полезные сервисы, которые нужно переиспользовать, а не дублировать:
  - `PointService`;
  - `CollectionService`;
  - `PointFilterService`;
  - `SearchService`;
  - сервисы вложений;
  - модели карты и документов.

## Не делаем в рамках этого roadmap

- Не меняем выбранный стек: WPF + BlazorWebView/WebView2.
- Не переносим приложение на web/mobile/cross-platform.
- Не меняем провайдера карт.
- Не добавляем базу данных.
- Не делаем enterprise-архитектуру с лишними слоями ради слоев.
- Не добавляем новые пользовательские функции, пока не стабилизирован текущий MVP.

## Этап 0. Baseline перед рефакторингом

### Цель

Зафиксировать, что именно уже работает, и получить точку сравнения перед переносом логики.

### Статус

- Автоматическая baseline-проверка выполнена 2026-07-08.
- Карта ответственности создана: `docs/appshell-responsibility-map.md`.
- Ручной smoke-check по `docs/MANUAL_TESTING.md` еще требует живого прохода в UI.

### Работы

- [x] Проверить, что проект собирается локально.
- [x] Прогнать текущие автоматические тесты.
- [ ] Выполнить ручной smoke-check по `docs/MANUAL_TESTING.md`.
- [x] Зафиксировать текущие известные ограничения в `docs/known-limitations.md`.
- [x] Составить короткую карту ответственности `AppShell.razor`: какие блоки кода за какие сценарии отвечают.

### Критерии приемки

- [x] Есть понятное описание текущего состояния.
- [x] Известно, какие сценарии нельзя сломать.
- [x] Есть список рисковых зон перед выносом логики.

## Этап 1. Тестовый каркас для безопасного рефакторинга

### Цель

Перед переносом логики закрыть тестами самые дорогие для поломки сценарии.

### Сценарии для покрытия

- [ ] Создание точки без вложений.
- [ ] Создание точки с коллекциями.
- [ ] Создание точки с фото и обычными файлами.
- [ ] Редактирование точки.
- [ ] Удаление точки с подтверждением.
- [ ] Создание, редактирование и удаление коллекции.
- [ ] Поиск по названию, адресу, региону, описанию и тегам.
- [ ] Фильтрация по коллекциям в режимах И/ИЛИ.
- [ ] Фильтрация по тегам в режимах И/ИЛИ.
- [ ] Комбинация поиска и фильтров.

### Ошибочные сценарии

- [ ] Невалидная строка тегов.
- [ ] Файл с расширением картинки, который не является картинкой.
- [ ] Отсутствующий файл вложения.
- [ ] Ошибка записи в папку библиотеки.
- [ ] Битый JSON точки или коллекции.

### Критерии приемки

- [ ] Ключевые сценарии проверяются автоматическими тестами.
- [ ] Тесты проверяют поведение приложения, а не только отдельные приватные методы.
- [ ] Перед началом этапа 2 есть защита от тихих регрессий.

## Этап 2. Вынос логики из Razor-компонента

### Цель

Сделать `AppShell.razor` тонким экраном, а состояние и команды перенести в понятные App-level VM/сервисы.

Важно: UI-состояние не уходит в `Core`. В `Core` остаются чистые правила и работа с данными. Состояние экрана, dirty-state, выбранные панели, результаты поиска и ошибки живут в приложении.

### Целевая схема

- `AppShell.razor`
  - общая компоновка экрана;
  - подключение дочерних компонентов;
  - биндинги;
  - вызов команд VM/сервисов.
- App-level VM/сервисы
  - состояние экрана;
  - команды пользователя;
  - координация между UI и `Core`;
  - подготовка данных для отображения.
- `Core`
  - документы точек и коллекций;
  - чтение и запись файлов библиотеки;
  - поиск;
  - фильтрация;
  - нормализация тегов;
  - правила primary collection;
  - импорт и классификация вложений.

### Подэтап 2.1. Поиск

- [ ] Вынести состояние поиска в `SearchState` или `SearchViewModel`.
- [ ] Перенести туда:
  - текст поиска;
  - флаг активности кнопки `Search`;
  - запуск поиска только по кнопке;
  - сброс поиска;
  - список найденных точек.
- [ ] Оставить в Razor только поле ввода, кнопки и список результатов.

Критерии приемки:

- [ ] Пустой или пробельный запрос делает кнопку поиска неактивной.
- [ ] Очистка поля сама не меняет карту до нажатия кнопки.
- [ ] Клик по результату центрирует карту без изменения zoom.
- [ ] Поведение совпадает с текущим.

### Подэтап 2.2. Фильтры

- [ ] Вынести состояние фильтров в `FilterState` или `FilterViewModel`.
- [ ] Перенести туда:
  - выбранные коллекции;
  - порядок выбранных коллекций;
  - drag-and-drop reorder;
  - режим коллекций И/ИЛИ;
  - выбранные теги;
  - режим тегов И/ИЛИ;
  - отдельный reset коллекций;
  - отдельный reset тегов.
- [ ] Использовать существующий `PointFilterService` для фактического отбора точек.

Критерии приемки:

- [ ] Фильтр коллекций поддерживает мультивыбор.
- [ ] Фильтр тегов поддерживает мультивыбор.
- [ ] У коллекций и тегов независимые режимы И/ИЛИ.
- [ ] Порядок выбранных коллекций влияет на выбор иконки при мульти-коллекциях.
- [ ] Reset коллекций не сбрасывает теги, reset тегов не сбрасывает коллекции.

### Подэтап 2.3. Правая панель точки

- [x] Вынести состояние редактирования точки в `PointEditorViewModel`.
- [ ] Перенести туда:
  - [x] режим создания/редактирования;
  - [x] выбранную точку;
  - [x] координаты новой точки;
  - [x] поля формы;
  - [x] dirty-state;
  - [x] confirm discard changes;
  - [x] save;
  - [x] delete;
  - [x] ошибки формы.
- [ ] Razor должен только отображать форму и вызывать команды.

Критерии приемки:

- [ ] Клик по сохраненному пину открывает редактирование точки.
- [ ] Клик по карте ставит временную координату.
- [ ] Кнопка `Добавить точку` активна только после выбора координаты.
- [ ] `Esc` сбрасывает временный маркер и координаты.
- [ ] При несохраненных изменениях показывается `Discard changes`.
- [ ] Удаление точки требует подтверждения.

### Подэтап 2.4. Коллекции

- [x] Вынести создание, редактирование и удаление коллекций в `CollectionEditorViewModel`.
- [x] Перенести туда:
  - [x] форму коллекции;
  - [x] выбор иконки;
  - [x] выбор цвета;
  - [x] dirty-state;
  - [x] save;
  - [x] delete до тонкого confirm-wrapper;
  - [x] ошибки.
- [x] Использовать существующий `CollectionService`.

Критерии приемки:

- [ ] Коллекции создаются и редактируются в левой панели.
- [ ] Иконка выбирается из библиотеки.
- [ ] Цвет выбирается через стандартный color picker.
- [ ] Удаление коллекции требует подтверждения.
- [ ] Ошибки объясняют, что сломалось и как починить.

### Подэтап 2.5. Вложения

- [x] Вынести draft-состояние вложений в `AttachmentDraftState`.
- [ ] Перенести туда:
  - [x] pending-файлы;
  - [x] классификацию фото/файл;
  - [x] импорт только при `Save`;
  - [x] удаление pending-вложений;
  - [x] удаление сохраненных вложений;
  - [x] открытие файлов;
  - [x] ошибки вложений.
- [x] Переиспользовать существующие сервисы вложений.

Критерии приемки:

- [ ] Файлы копируются в библиотеку только после `Save`.
- [ ] Один плохой файл не отменяет хорошие.
- [ ] `.jpg`, `.jpeg`, `.png`, `.webp`, `.gif` проверяются как реальные изображения.
- [ ] Переименованный не-image файл сохраняется как обычный файл и не показывается в галерее.
- [ ] Файлы открываются по клику.

### Подэтап 2.6. Разделение Razor-компонентов

- [x] Выделить дочерние компоненты без изменения поведения:
  - [x] `LeftPanel.razor`;
  - [x] `SearchPanel.razor`;
  - [x] `CollectionsPanel.razor`;
  - [x] `TagsPanel.razor`;
  - [x] `MapWorkspace.razor`;
  - [x] `PointPanel.razor`;
  - [x] `AttachmentPanel.razor`;
  - [x] `StatusBar.razor`;
  - [x] `SettingsPanel.razor`.
- [x] Не создавать вложенные карточки и лишнюю декоративную структуру.
- [x] Сохранить плотный рабочий интерфейс.

Критерии приемки:

- [x] `AppShell.razor` стал заметно меньше и читабельнее.
- [x] Каждый компонент имеет понятную ответственность.
- [x] В компонентах нет дублирования бизнес-правил.

## Этап 3. Единая модель ошибок в UI

### Цель

Сделать ошибки понятными пользователю и одинаковыми по стилю во всех основных сценариях.

### Работы

- [ ] Ввести единый формат UI-ошибки:
  - краткий заголовок;
  - понятное описание;
  - техническая причина, если полезно;
  - действие для исправления.
- [ ] Применить формат к ошибкам:
  - сохранения точки;
  - сохранения коллекции;
  - импорта вложений;
  - удаления вложений;
  - открытия файла;
  - выбора библиотеки;
  - загрузки битого JSON.
- [ ] Добавить состояния операций:
  - Idle;
  - Loading;
  - Success;
  - Error.
- [ ] Добавить `Retry` только для безопасных операций, где повтор не создает дублей.

### Критерии приемки

- [ ] Пользователь видит, что именно сломалось.
- [ ] Пользователь понимает, что можно сделать дальше.
- [ ] Системные исключения не вываливаются сырым текстом в UI.

## Этап 4. Файловое хранилище: версии, миграции, backup

### Цель

Подготовить локальную файловую библиотеку к будущим изменениям формата без риска потерять пользовательские данные.

### Работы

- [ ] Добавить версию формата библиотеки.
- [ ] Описать, где хранится версия:
  - настройки приложения в `%APPDATA%\ChoKuda`;
  - данные библиотеки в выбранной пользователем папке.
- [ ] Описать стратегию миграций:
  - определить текущую версию;
  - проверить, нужна ли миграция;
  - сделать backup;
  - выполнить миграцию;
  - проверить результат.
- [ ] Описать формат backup:
  - что копируем;
  - куда копируем;
  - как называем backup;
  - как пользователь может восстановиться вручную.

### Критерии приемки

- [ ] Перед опасным изменением формата есть backup.
- [ ] Старый формат можно распознать.
- [ ] Миграции можно добавлять последовательно.
- [ ] Пользовательские файлы не трогаются без необходимости.

## Этап 5. Контроль качества перед новым функционалом

### Цель

Не наращивать новые возможности поверх хрупкого экрана.

### Проверки перед началом новых features

- [ ] `AppShell.razor` больше не является местом, куда добавляется вся логика подряд.
- [ ] Основные сценарии покрыты тестами.
- [ ] Ручной smoke-check проходит.
- [ ] Ошибки в UI читаемые.
- [ ] Хранилище имеет понятную стратегию версий и backup.
- [ ] README и planning-документы соответствуют фактическому состоянию проекта.

## Рекомендуемый порядок выполнения

1. Этап 0: baseline.
2. Этап 1: тестовый каркас.
3. Подэтап 2.1: поиск.
4. Подэтап 2.2: фильтры.
5. Подэтап 2.3: правая панель точки.
6. Подэтап 2.4: коллекции.
7. Подэтап 2.5: вложения.
8. Подэтап 2.6: разделение Razor-компонентов.
9. Этап 3: единая модель ошибок.
10. Этап 4: версии, миграции, backup.
11. Этап 5: контроль качества перед новым функционалом.

## Первый рабочий спринт

### Спринт 1.1. Baseline и карта ответственности

- [x] Собрать проект.
- [x] Прогнать тесты.
- [ ] Выполнить ручной smoke-check.
- [x] Разметить ответственность текущего `AppShell.razor`.
- [x] Зафиксировать список сценариев, которые должны остаться неизменными.

Результат: понятно, что именно защищаем перед рефакторингом.

### Спринт 1.2. Тесты поиска и фильтрации

- [x] Покрыть поиск.
- [x] Покрыть фильтр коллекций.
- [x] Покрыть фильтр тегов.
- [x] Покрыть комбинацию поиска и фильтров.

Результат: можно безопасно выносить search/filter state.

### Спринт 1.3. Вынос поиска и фильтров

- [x] Создать VM/State для поиска.
- [x] Создать VM/State для фильтров.
- [x] Подключить их к `AppShell.razor`.
- [ ] Проверить, что поведение карты, результатов и reset-кнопок не изменилось.

Результат: первый реальный кусок логики вынесен из Razor без изменения поведения.

### Спринт 1.4. Первый вынос правой панели точки

- [x] Создать `PointEditorViewModel`.
- [x] Перенести form/snapshot/new-edit/dirty-state.
- [x] Перенести правила выбора коллекций точки и primary collection.
- [x] Перенести ошибки формы точки в `PointEditorViewModel`.
- [x] Перенести базовые VM-команды save/delete без UI-confirm и вложений.
- [x] Покрыть `PointEditorViewModel` app-level тестами.
- [x] Вынести draft/display/error-состояние вложений в `AttachmentDraftState`.
- [x] Покрыть `AttachmentDraftState` app-level тестами.
- [x] Перенести attachment-aware save orchestration из `AppShell`.
- [x] Перенести saved attachment delete flow из `AppShell`.
- [x] Перенести delete UI-flow из `AppShell` до тонкого wrapper.
- [x] Перенести временную координату новой точки в `PointEditorViewModel`.
- [x] Перенести discard-check правой панели до тонкого JS-confirm wrapper.
- [ ] Выполнить ручной smoke-check правой панели.

Результат: состояние формы точки, draft-состояние вложений, attachment-aware save workflow, saved attachment delete flow и delete flow точки вынесены до тонких UI-wrapper'ов.

## Метрики успеха

- [ ] Размер `AppShell.razor` заметно уменьшился.
- [ ] Новая логика не добавляется напрямую в основной Razor-компонент.
- [ ] Основные сценарии защищены тестами.
- [ ] Ошибки в UI понятны без чтения stack trace.
- [ ] Файловая библиотека имеет план безопасных изменений формата.
- [ ] После каждого этапа приложение остается рабочим.
